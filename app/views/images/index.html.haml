%p#notice= notice
%h1 Listing Images
%table
  %thead
    %tr
      %th Link
      %th Product
      %th{:colspan => "3"}
  %tbody
    - @images.each do |image|
      %tr
        %td= image.link
        %td= image.product.name
        %td= link_to 'Show', image
        %td= link_to 'Delete', image, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Image', new_product_image_path(@product)
= link_to 'Back to Product', product_path(@product)
